function mostrar()
{
	var contador;
	contador=0; //va desde cero a 10
	//while( la primera vez tiene que ser verdadero) {} una vez que termina vuelve arriba a evaluar
	while (contador<10) //se recorre 5 veces

	{
		contador++; // le esta sumando un numero
		console.log("Hola "+contador);
		//contador= contador+1; //modificar el contador
		 //incrementa 1 a esto. Segun donde ponga el contador me afecta el mensaje
	}

}//FIN DE LA FUNCIÓN